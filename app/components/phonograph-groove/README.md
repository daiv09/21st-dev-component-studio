# Phonograph Groove

Spiral groove platter with stylus arm and live scope of lateral modulation.

**Tags:** `phonograph` `spiral` `audio` `canvas` `stylus`

## Math

```
r(θ) = r₀ − (pitch·θ)/(2π) + m·sin(kθ)
```

Stylus scope plots m·sin(kθ) + harmonics as the record rotates at ω.
