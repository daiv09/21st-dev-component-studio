# Retractable Tape

Spring-return measuring tape with lock pawl — extend against torsion, release to retract.

**Tags:** `spring` `mechanism` `svg` `physics` `tool`

## Math

Unlocked: `ẍ = −k·x − cẋ` (retract to case). Pawl freezes state. Blade ticks every 10 cm; drag hook sets length while captured.
